import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Home = () => {
    return (
        <SafeAreaView>
            <Text className="text-general-400">Home</Text>
        </SafeAreaView>
    );
};

export default Home;
