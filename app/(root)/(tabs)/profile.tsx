import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Profile = () => {
    return (
        <SafeAreaView>
            <Text className="text-general-400">Profile</Text>
        </SafeAreaView>
    );
};

export default Profile;
