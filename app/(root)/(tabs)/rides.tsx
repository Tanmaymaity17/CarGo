import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Rides = () => {
    return (
        <SafeAreaView>
            <Text className="text-general-400">Rides</Text>
        </SafeAreaView>
    );
};

export default Rides;
