import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const SignUP = () => {
    return (
        <SafeAreaView>
            <Text className="text-general-400">SignUP</Text>
        </SafeAreaView>
    );
};

export default SignUP;
