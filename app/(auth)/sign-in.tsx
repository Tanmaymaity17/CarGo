import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const SignIn = () => {
    return (
        <SafeAreaView>
            <Text className="text-general-400">SignIn</Text>
        </SafeAreaView>
    );
};

export default SignIn;
